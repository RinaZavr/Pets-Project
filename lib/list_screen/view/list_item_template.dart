import 'package:flutter/material.dart';

class ListItemTemplate extends StatelessWidget {
  const ListItemTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
