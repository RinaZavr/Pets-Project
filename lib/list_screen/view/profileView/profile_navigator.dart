import 'package:flutter/cupertino.dart';

class ProfileNavigator extends StatelessWidget {
  const ProfileNavigator({super.key});

  static const String initialRoute = 'profile_view';

  @override
  Widget build(BuildContext context) {
    return Navigator(
    );
  }
}